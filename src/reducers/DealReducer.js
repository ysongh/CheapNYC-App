const initialState = {
    deal: [],
    loading: true,
    showModal: false
}

export default (state = initialState, action) => {
    switch(action.type){
        default:
            return state;
    }
}